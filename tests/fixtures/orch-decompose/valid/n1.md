### n1. Schema + loader
**Goal:** add schema + loader.
**Files:**
- `db/schema.rb` (create)
- `lib/loader.rb` (create)
**Approach:** add the table and a loader.
**Test scenarios:**
- loads rows from source
**Verification:** rows present in db.
